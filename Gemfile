source "https://rubygems.org"

# A lightweight local preview setup. GitHub Pages uses its own pinned versions;
# this is only for running `bundle exec jekyll serve` locally.
gem "jekyll", "~> 3.9"
gem "minima", "~> 2.5"

# Ruby 3+ needs webrick; harmless on older Rubies.
gem "webrick"

